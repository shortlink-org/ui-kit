include:
  - local: .gitlab/ci/workflows/matrix_build_npm.yml
  - local: .gitlab/ci/templates/cache.yml

npm:
  stage: publish
  image: node:24-alpine

  # --------------------------- variables ----------------------------
  variables:
    NPM_TOKEN: "$CI_JOB_TOKEN"
    # Allow overriding where the package.json lives
    PACKAGE_PATH: "${PACKAGE_PATH:-.}"
    NPM_CONFIG_PROVENANCE: "true"
    # Dist-tag to use when publishing a SHA build
    CANARY_TAG: "canary"

  # ------------------------ auth + tooling --------------------------
  before_script:
    # ↳ Auth for GitLab package registry
    - printf "//%s/api/v4/projects/%s/packages/npm/:_authToken=%s\n" \
        "$CI_SERVER_HOST" "$CI_PROJECT_ID" "$CI_JOB_TOKEN" >> ~/.npmrc
    - echo "@${CI_PROJECT_ROOT_NAMESPACE}:registry=${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/npm/" >> ~/.npmrc

    # ↳ pnpm
    - corepack enable
    - corepack prepare pnpm@latest --activate

    # ↳ move to the package
    - cd "$PACKAGE_PATH"


  # -------------------------- main logic ----------------------------
  script:
  - |
    set -e

    # ------------------ decide version ------------------
    if [ -n "$CI_COMMIT_TAG" ] &&
        echo "$CI_COMMIT_TAG" | grep -Eq '^v?[0-9]+\.[0-9]+\.[0-9]+(-.*)?$'; then
      VERSION=$(echo "$CI_COMMIT_TAG" | sed 's/^v//')
      DIST_TAG="latest"
    elif [ "$CI_COMMIT_BRANCH" = "main" ]; then
      DATE=$(date -u '+%Y%m%d')
      TIME=$(date -u '+%H%M%S')
      VERSION="0.${DATE}.${TIME}"          # 0.YYYYMMDD.HHMMSS
      DIST_TAG="$CANARY_TAG"
    else
      echo "⏩ Not a SemVer tag and not main – skipping publish."
      exit 0
    fi

    echo "📦 Publishing $VERSION (dist-tag: $DIST_TAG)"

    # bump package.json without committing
    pnpm version --no-git-tag-version "$VERSION"

    # publish – ignore 'version exists' errors
    if ! npm publish \
          --registry "${CI_API_V4_URL}/projects/${CI_PROJECT_ID}/packages/npm/" \
          --scope=@shortlink-org \
          --access public \
          --tag "$DIST_TAG"; then
      echo "⚠️  Version $VERSION already exists – skipping."
    fi

  # Run only on tags or on main
  rules:
    - if: '$CI_COMMIT_TAG'
    - if: '$CI_COMMIT_BRANCH == "main"'
    - when: never          # all other refs skip the job

  retry: 2
  allow_failure: false
